Important steps and process

- conda create -n gpt-assistant python=3.10
- conda activate gpt-assistant
- pip install langchain openai streamlit faiss-cpu tiktoken pyPDF2 python-dotenv

GIT added:- 

echo "# GPT-Assistant" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Satyask-ai/GPT-Assistant.git
git push -u origin main

git remote add origin https://github.com/Satyask-ai/GPT-Assistant.git
git branch -M main
git push -u origin main

code for prompts had errors(step1)

code executed successfully( api-key trail version expired, next step but the API-KEY)